Here is the content of the provided PDF file converted into Markdown format.

---

# راهنمای سرویس نسخه نویسی الکترونیک سازمان تامین اجتماعی

**نسخه 1.9.4**  
**تاریخ آخرین تغییرات: 1405 اردیبهشت**

کلیه حقوق این سند متعلق به سازمان تامین اجتماعی می باشد و هرگونه کپی برداری و استفاده غیرمجاز از آن قابل پیگیری می باشد.

---

## شناسنامه سند

| عنوان | توضیح |
| :--- | :--- |
| **نام سند** | راهنمای سرویس نسخه نویسی الکترونیک سازمان تامین اجتماعی |
| **نگارش** | 1.9.3 |
| **تاریخ صدور** | 1405/02/15 |
| **شرح سند** | این سند تشریحی است بر چگونگی ارسال نسخه الکترونیک تجویزی پزشک به سرویس سازمان تامین اجتماعی |

---

## تاریخچه تغییرات سند

| تاریخ | نسخه | توضیحات | تهیه کننده |
| :--- | :--- | :--- | :--- |
| 1399/5/15 | 1.0 | تهیه سند اولیه | مدیریت توسعه سامانه های درمانی |
| 1399/10/20 | 1.1 | اصلاح آدرس سرویس های اطلاعات پایه | مسئول پروژه نسخه الکترونیک |
| 1399/11/08 | 1.2 | تغییر مسیر دریافت توکن | مسئول پروژه نسخه الکترونیک |
| 1399/11/09 | 1.2 | افزودن متد استعلام وضعیت استحقاق | مسئول پروژه نسخه الکترونیک |
| 1400/03/26 | 1.3 | افزودن متدهای فراخوانی، حذف و ویرایش نسخه | مسئول پروژه نسخه الکترونیک |
| 1401/02/20 | 1.4 | تغییر پارامترهای نسخ فیزیوتراپی، نحوه ثبت نسخ با استفاده از کدینگ اعلام وزارت بهداشت | مسئول پروژه نسخه الکترونیک |
| 1402/10/01 | 1.5 | افزودن سرویس های مرتبط با نسخ دندانپزشکی، افزودن سرویس های نظام ارجاع | مسئول پروژه نسخه الکترونیک |
| 1403/04/04 | 1.6 | افزودن سرویس های ثبت نسخه پرستاری و کارتابل پرستاری | مسئول پروژه نسخه الکترونیک |
| 1403/05/28 | 1.7 | تغییر سرویس احراز هویت | مسئول پروژه نسخه الکترونیک |
| 1403/07/10 | 1.8 | تغییر ساختار مستندات | مسئول پروژه نسخه الکترونیک |
| 1404/02/18 | 1.9 | حذف کد تائید از سرویس های ویرایش و ... | مسئول پروژه نسخه الکترونیک |
| 1404/03/05 | 1.9.1 | افزودن سرویس های دریافت و مشاهده ... | مسئول پروژه نسخه الکترونیک |
| 1404/03/05 | 1.9.2 | افزودن سرویس خروج کاربران، امکان ثبت ... | مسئول پروژه نسخه الکترونیک |
| 1404/07/25 | 1.9.3 | شکایت برای نسخ و تشخیص اولیه برای اقلام ... | مسئول پروژه نسخه الکترونیک |
| 1404/08/27 | 1.9.4 | نسخه، افزودن سرویس های جدید ارجاع ... | مسئول پروژه نسخه الکترونیک |
| 1405/02/15 | 1.9.4 | افزودن سرویس فراخوانی نسخه باز ارجاعی بیمار توسط منشی | مسئول پروژه نسخه الکترونیک |
| 1405/02/15 | 1.9.4 | افزودن امکان ثبت نسخه بستری، افزودن سرویس مشاهده نسخ دستور بستری | مسئول پروژه نسخه الکترونیک |

### تغییرات این نسخه:

1.  تغییر آدرس سرویس احراز هویت در محیط تست
2.  افزودن امکان ثبت نسخه دستور بستری در سرویس ثبت نسخ الکترونیک
3.  افزودن سرویس مشاهده لیست نسخ دستور بستری در درمانگاه
4.  افزودن روال تجدید توکن در سرویس احراز هویت

---

## فهرست مطالب

- [1- مقدمه](#1-مقدمه)
- [2- سرویس احراز هویت](#2--سرویس-احراز-هویت)
- [3- سرویس ثبت نسخ الکترونیکی](#3-سرویس-ثبت-نسخ-الکترونیکی)
- [4- سرویس استحقاق سنجی بیمار (استعلام وضعیت استحقاق)](#4-سرویس-استحقاق-سنجی-بیماراستعلام-وضعیت-استحقاق)
- [5- سرویس واکنشی نسخه](#5-سرویس-واکنش-نسخه)
- [6- سرویس حذف نسخه](#6-سرویس-حذف-نسخه)
- [7- سرویس ویرایش نسخه](#7-سرویس-ویرایش-نسخه)
- [8- سرویس های اطلاعات پایه](#8-سرویس-های-اطلاعات-پایه)
- [9- سرویس های مرتبط با دندانپزشکی](#9--سرویس-های-مرتبط-با-دندانپزشکی)
    - [1-9 سرویس بررسی قواعد دندانپزشکی](#1-9-سرویس-بررسی-قواعد-دندانپزشکی)
    - [2-9 سرویس های پایه مربوط به دندانپزشکی](#2-9-سرویس-های-پایه-مربوط-به-دندانپزشکی)
        - [1-2-1 سرویس اول](#1-2-1-سرویس-اول)
        - [2-2-1 سرویس دوم](#2-2-1-سرویس-دوم)
- [10- سرویس های مرتبط با ارجاع](#10--سرویس-های-مرتبط-با-ارجاع)
    - [1-10 سرویس واکشی تعداد نسخه باز](#1-10-سرویس-واکشی-تعداد-نسخه-باز)
    - [2-10 سرویس واکشی نسخه ارجاع پزشک](#2-10-سرویس-واکشی-نسخه-ارجاع-پزشک)
    - [3-10 سرویس واکنش نسخ دارای بازخورد ارجاع](#3-10-سرویس-واکنش-نسخ-دارای-بازخورد-ارجاع)
    - [4-10 سرویس دریافت لیست نسخ ارجاعی پزشک در 3 روز اخیر](#4-10-سرویس-دریافت-لیست-نسخ-ارجاعی-پزشک-در-3-روز-اخیر)
    - [5-10 سرویس های نمایش بازخورد ارجاع](#5-10-سرویس-های-نمایش-بازخورد-ارجاع)
        - [1-5-10 سرویس واکنشی نسخه ارجاع ثبت شده بیمار](#1-5-10-سرویس-واکنشی-نسخه-ارجاع-ثبت-شده-بیمار)
        - [2-5-10 سرویس دریافت لیست نسخ بازخورد ارجاع ثبت شده](#2-5-10-سرویس-دریافت-لیست-نسخ-بازخورد-ارجاع-ثبت-شده)
        - [3-5-10 سرویس دریافت جزئیات بازخورد ارجاع ثبت شده](#3-5-10-سرویس-دریافت-جزئیات-بازخورد-ارجاع-ثبت-شده)
    - [6-10 سرویس فراخوانی نسخ باز ارجاعی بیمار توسط منشی](#6-10-سرویس-فراخوانی-نسخ-باز-ارجاعی-بیمار-توسط-منشی)
- [11- سرویس های مرتبط با کارتابل پرستاری](#11--سرویس-های-مرتبط-با-کارتابل-پرستاری)
    - [1-11 سرویس واکنش نسخ پرستاری درمانگاه](#1-11--سرویس-واکنش-نسخ-پرستاری-درمانگاه)
    - [2-11 سرویس ثبت اقدام روی نسخه پرستاری](#2-11--سرویس-ثبت-اقدام-روی-نسخه-پرستاری)
- [12- سرویس های مرتبط با خدمات ارجاعی](#12--سرویس-های-مرتبط-با-خدمات-ارجاعی)
    - [1-12 سرویس واگشی نسخه خدمات ارجاعی](#1-12-سرویس-واگشی-نسخه-خدمات-ارجاعی)
- [13- سرویس های مرتبط با بستری](#13--سرویس-های-مرتبط-با-بستری)
    - [1-13 سرویس واکنش لیست نسخ بستری در درمانگاه](#1-13-سرویس-واکنش-لیست-نسخ-بستری-در-درمانگاه)
- [14- نکات مهم](#14--نکات-مهم)

---

## ۱- مقدمه

امروزه اطلاعات علمی و فنی اساس توانائی های مادی و معنوی جوامع بشری بوده و هر جامعه ای که بتواند به اطلاعات بیشتری دست یابد از توان و پویایی بیشتری نیز برخوردار خواهد شد. فناوری اطلاعات و ارتباطات به عنوان یکی از بسترهای نوین به سرعت در حال تأثیر گذاری بر زندگی بشر است و این تأثیر در کلیه وجوه اجتماعی دیده می شود. در عصر تکنولوژی، اطلاعات، بخش حیاتی برای ارائه مراقبت بهداشتی و درمانی است. سازمان تامین اجتماعی به عنوان سازمان پیشرو در اجرای طرح نسخه الکترونیک به عنوان هسته اصلی پرونده الکترونیک سلامت، بر آن است با استقرار پروتکل تبادل اطلاعات با سایر تولیدکنندگان نسخه الکترونیک گام های موثری در راستای تکمیل طرح مذکور برداشته شود.

این سند به منظور تشریح چگونگی ارسال نسخ الکترونیک تجویزی پزشک برای بیمه شده سازمان تامین اجتماعی می باشد. کلیه نرم افزارهای مطب دارای مجوز و گواهینامه های لازمه امکان بهره برداری از این سرویس را خواهند داشت.

## 2 - سرویس احراز هویت

احراز هویت با روش PKCE صورت می گیرد که برای دریافت توکن از سرور، از یک مقدار که توسط کلاینت ساخته شده، استفاده می شود. در این روش یک کد جدید توسط کلاینت ساخته می شود و به همراه درخواست ارسال می شود. سپس دوباره authorization-code به سرور ارسال می شود تا اعتبار سنجی صورت گیرد. مراحل کلی درخواست به صورت زیر می باشد:

- ارسال درخواست جهت دریافت authorization-code
- ارسال درخواست برای دریافت توکن

<center>*نحوه تولید code verifier و code challenge</center>

کلاینت برای دریافت authorization code باید یک کد تولید کند. این کد **code verifier** نام دارد که رشته ای تصادفی است شامل [A-Z][a-z][0-9]--~ کاراکتر و از ترکیب کاراکترهای 43 تا 128 کاراکتر.

| عنوان | توضیح |
| :--- | :--- |
| **CODE VERIFIER** | |
| تعداد کاراکتر های مجاز | 43 تا 128 کاراکتر |
| کاراکتر های مجاز | [a-z][A-Z][0-9]"-" |

#### Code Challenge:

زمانی که code service، code_challenge را دریافت می‌کند، آن را با استفاده از SHA256 و سپس BASE64-URL-ENCODE رمزگذاری می‌کند.  
`BASE64URL-ENCODE(SHA256(ASCII(code_verifier))) == code_challenge`

با توجه به توضیحات بالا و نحوه تولید کدهای ذکر شده، از دو سرویس زیر جهت دریافت توکن استفاده می‌شود.

### -1 ارسال درخواست برای دریافت authorization_code

سرویس باید با متد GET فراخوانی شود و باید به آدرس زیر درخواست ارسال گردد. با فراخوانی این سرویس خروجی، به شکل html خواهد بود و در واقع پزشکان به درگاه ورود کاربران سازمان تامین اجتماعی ارجاع داده می شوند تا گذرواژه و نام کاربری خود را وارد نمایند.

**آدرس محیط عملیاتی:** `https://account.tamin.ir/auth/server/authorize`  
**آدرس محیط تستی:** `https://account-pilot.tamin.ir/auth/server/authorize`

پارامترهای زیر نیز به عنوان QueryParam باید اضافه گردند:

- `redirect_uri`: آدرس بازگشت سایت یا آدرسی که کاربر به این صفحه ارجاع داده خواهد شد.
- `code_challenge`: این مقدار باید توسط کلاینت ساخته شود که در ادامه درباره آن توضیح داده شده است.
- `client_id`: شناسه کلاینت که قصد دریافت توکن را دارد.
- `response_type`: برای این پارامتر مقدار `code` را مقداردهی شود.
- `code_challenge_method`: برای این پارامتر مقدار `S256` را مقداردهی شود.

**نمونه فراخوانی سرویس به شکل زیر خواهد بود:**

`https://account.tamin.ir/auth/server/authorize?redirect_uri=https://ep.tamin.ir/view/index.html&response_type=code&client_id=12345...12345a&code_challenge=VILZG3tR4MuMQXbySv7OQTmM0Ez9SrtVR o8iBaRvPLQ&code_challenge_method=S256`

نهایتا پس از ورود پزشک و وارد کردن نام کاربری و گذرواژه، خروجی مشابهی در آدرس بازگشتی وارد شده خواهد داشت که مقدار code باید در سرویس بعدی ارسال شود.

`http://redirectUri/?code=eyJhbGciOiJSUzI1NiI********Fgerty2R&expires_in=60`

**نکته مهم:** جهت تشخیص کاربر فعلی که در حال احراز هویت است (یا هر داده دیگه که جهت تشخیص و تمایز بین کاربران یا سیستم های مختلف استفاده می شود) پارامتر `state` به ورودی های این سرویس باید اضافه گردد و عملکرد آن به این صورت می باشد که در آدرس بازگشتی علاوه بر پارامترهای دیگر این پارامتر نیز با همان مقداری که ارسال شده است، قابل دریافت می باشد. به عنوان مثال اگر سرویس اول به صورت زیر فراخوانی شود:

`https://account.tamin.ir/auth/server/authorize?redirect_uri=https://ep.tamin.ir &response_type=code&client_id=12345...12345a&code_challenge=VILZG3tR4MuMQXbySv7OQTmM 0Ez9SrtVRo8iBaRvPLQ&code_challenge_method=S256&state=1234567891`

پس از اتمام احراز هویت پزشک در درگاه سازمان تامین اجتماعی، موقع بازگشت به آدرس بازگشتی به صورت زیر خواهد بود:

`http://[redirectUri]/?code=eyJhbGciOiJSJzI1NiI*******Fgerty2R&expires_in=60&state=1234567891`

### 2- دریافت توکن

در ادامه جهت دریافت توکن، باید وب سرویس زیر از طریق متد POST فراخوانی گردد. در فراخوانی این سرویس ورودی ها باید به صورت x-www-form-urlencoded ارسال گردد.

**آدرس محیط عملیاتی:** `https://account.tamin.ir/auth/server/token`  
**آدرس محیط تستی:** `https://account-pilot.tamin.ir/auth/server/token`

**پارامترهای ورودی:**

- `redirect_uri`
- `grant_type`: باید رشته "authorization_code" ارسال گردد.
- `client_id`
- `code`: کد دریافت شده در سرویس قبل که به آن اشاره شد.
- `code_verifier`: مقدار ساخته شده که طریقه ساختن آن توضیح داده شد.

**خروجی سرویس** همانند زیر خواهد بود و توکن جهت استفاده در سایر سرویس ها آماده استفاده می باشد.

```json
{
  "access_token": "eyJhbGciOiJSJzI1NiJ9.dWiOiIwfTUxq1RdefWIgCNDUxM....",
  "expires_in": 3600
}
```

### 3- سرویس خروج از سامانه

از این سرویس جهت خروج از سامانه نسخه نویسی تامین اجتماعی استفاده می شود و باید زمانی که کاربر قصد خروج از سیستم را دارد فراخوانی گردد. نحوه فراخوانی این سرویس به این شکل می باشد که آدرس بازگشتی اعلام شده را به عنوان ورودی دریافت می کند و با متد GET باید فراخوانی گردد.

**آدرس محیط عملیاتی:** `https://account.tamin.ir/auth/signout?redirect_uri=https://ep.tamin.ir`  
**آدرس محیط تستی:** `https://account-pilot.tamin.ir/auth/signout?redirect_uri=https://ep.tamin.ir`

**پارامترهای ورودی:**

- `redirect_uri`: آدرسی که به عنوان آدرس بازگشتی توسط شرکت اعلام شده است.

### 4- روال تجدید توکن

#### 1-4 سرویس تجدید توکن پزشک

جهت تجدید توکن یا استفاده از refresh token، پس از فراخوانی سرویس اول (دریافت authorization code) باید سرویس زیر با پارامترهایی که در ادامه آمده است فراخوانی گردد. سرویس با متد POST باید فراخوانی گردد.

**آدرس سرویس در محیط عملیاتی:** `https://account.tamin.ir/auth/server/v2/token`  
**آدرس سرویس در محیط تست:** `https://account-pilot.tamin.ir/auth/server/v2/token`

**پارامترهای ارسالی:**

- `redirect_uri`
- `grant_type`: باید رشته `authorization_code` ارسال گردد.
- `client_id`
- `code`
- `code_verifier`
- `audience`: اطلاعات فرد یا موجودیتی که برای وی توکن صادر میگردد. مانند کد ملی پزشک

در زیر یک نمونه curl از فراخوانی سرویس قرار داده شده است.

```bash
curl --location --request POST 'https://account.tamin.ir/auth/server/v2/token' \
--header 'Content-Type: application/x-www-form-urlencoded' \
--data-urlencode 'redirect_uri=xxx' \
--data-urlencode 'grant_type=authorization_code' \
--data-urlencode 'client_id=xxx' \
--data-urlencode 'code=eyJhbGciOiJSUzI' \
--data-urlencode 'code_verifier=R056ZkU2OTlMOFI5ekVvaU4wemRMZEtJTTNjVmlQeXg' \
--data-urlencode 'audience=1234567890'
```

در خروجی سرویس پارامترهای `access_token`, `refresh_token` قرار دارد که از `refresh_token` در سرویس بعدی جهت تجدید توکن استفاده خواهد شد.

#### 2-4 سرویس دریافت توکن جدید با استفاده از refresh_token

در ادامه جهت دریافت توکن جدید بدون نیاز به ارجاع پزشک به صفحه ورود، باید سرویس زیر با پارامترهای قید شده با متد POST فراخوانی گردد. نحوه ارسال اطلاعات در سرویس قبل و این سرویس باید با فرمت x-www-form-urlencoded باشد.

**آدرس سرویس در محیط عملیاتی:** `https://account.tamin.ir/auth/server/v2/token`  
**آدرس سرویس در محیط تست:** `https://account-pilot.tamin.ir/auth/server/v2/token`

**پارامترهای ارسالی:**

- `grant_type`: همانند سابق باید رشته `refresh_token` ارسال گردد.
- `client_id`
- `refresh_token`: دریافت شده از سرویس قبلی
- `audience`: اطلاعات فرد یا موجودیتی که برای وی توکن صادر میگردد، مانند کد ملی پزشک

**نمونه فراخوانی سرویس:**

```bash
curl --location --request POST 'https://account.tamin.ir/auth/server/v2/token' \
--header 'Content-Type: application/x-www-form-urlencoded' \
--data-urlencode 'grant_type=refresh_token' \
--data-urlencode 'client_id=xxxx' \
--data-urlencode 'refresh_token=eyJhbGciOiva2VuIiwiZXhwIjoxNzc4MDk5MDk0LCJjbGllbnRfaWQiOiI0ZjI5NDMgbQ' \
--data-urlencode 'audience=1234567890'
```

- توجه داشته باشید مقدار `client_id` در محیط تستی `portal-js` ارسال گردد.
- جهت ثبت نسخه در محیط تستی از اطلاعات پزشکی تستی زیر استفاده نمائید:
    - **شماره نظام پزشک تستی:** 2000200092
    - **شماره ملی پزشک تستی:** 1234567891
    - **شماره همراه پزشک تستی:** 09991111111

- تمامی شرکت ها جهت استفاده در محیط عملیاتی باید آدرس بازگشتی خود را از طریق ثبت تیکت اعلام نمایند. در آدرس اعلامی توجه داشته باشید امکان قراردادن پارامترهای داینامیک وجود ندارد و آدرس و پارامترها باید استاتیک باشد. در ضمن فقط امکان ثبت آدرس هایی که دارای گواهی SSL باشند وجود دارد و آدرس اعلامی باید https باشد. همچنین آدرس بازگشتی در محیط تستی نباید شامل پورت باشد.

## ۳- سرویس ثبت نسخ الکترونیکی

از این سرویس جهت ثبت انواع نسخ الکترونیکی استفاده می‌شود. این سرویس با متد POST فراخوانی می‌گردد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/SendEpresc`

ورودی این سرویس پارامترهای زیر می باشد.

| نام پارامتر | نوع | توضیحات | نمونه |
| :--- | :--- | :--- | :--- |
| patient | String | کد ملی بیمار | 14030717 |
| prescDate | String | تاریخ نسخه | 14030815 |
| expireDate | String | تاریخ انقضا نسخه | |
| docId | String | شماره نظام پزشکی (بدون علامت) | |
| docNationalCode | String | کدملی پزشک | |
| docMobileNo | String | تلفن همراه پزشک | |
| prescType | PrescType | نوع نسخه | |
| comments | String | توضیحات | |
| noteDetailEprsc | List< NoteDetailEprsc> | اقلام نسخه | |
| noteDetailsReferralList | List<NoteDetailsReferral> | فقط در نسخ ارجاع و بستری - اقلام ارجاع پزشک | |
| referralFeedbackId | Long | فقط در نسخ ارجاع - شناسه نسخه ارجاع پزشک | |
| complaint | Complaint | شکایت | |
| noteHeadEprscSupplementary | | | |

- جهت ثبت نسخه برای محیط تستی، علاوه بر پارامترهای بالا، فیلد clientId نیز باید ارسال گردد، که مقدار آن نیز کد ملی پزشک ارسال گردد.

#### ساختار پارامتر prescType

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| prescTypeId | Integer | 1: دارو<br/>2: پاراکلینیک<br/>3: ویزیت<br/>4: خدمات<br/>5: خدمات ارجاعی<br/>6: ارجاع پزشک<br/>7: بازخورد ارجاع پزشک<br/>8: دستور بستری |

#### ساختار پارامتر noteDetailEprsc

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| srvId | Service | کد خدمت |
| srvQty | Integer | تعداد خدمت |
| timesAday | TbDrugAmount | مقادیر مصرف (اجباری برای نسخه دارویی) |
| dose | String | دستورات متفرقه |
| repeat | String | دوره تکرار (نسخه دارویی) |
| dateDo | String | تاریخ موثر انجام |
| drugInstruction | DrugInstruction | زمان مصرف (اجباری برای نسخه دارویی) |
| illnessId | String | نوع بیماری (برای نسخ فیزیوتراپی) |
| planId | String | طرح درمان (برای نسخ فیزیوتراپی) |
| referenceStatus | Integer | |
| isDentalService | String | نوع خدمت دندانپزشکی (برای نسخ دندانپزشکی) |
| toothId | String | کد FDI (در صورت وابستگی خدمت به دندان) |
| DiagnosisID | | تشخیص اولیه (در حال حاضر صرفا برای نسخ دارویی) |

##### ساختار پارامتر Service

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| srvCode | String | کد خدمت |
| srvType | ServiceType | |
| parTarefGrp | ParTarefGroup | گروه آزمایش (اجباری برای نسخه های پاراکلینیک آزمایشگاه) |
| terminology | String | |

##### ساختار پارامتر ServiceType

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| srvType | String | 01: دارویی<br/>02: آزمایشگاه<br/>03: رادیولوژی<br/>04: سونوگرافی<br/>05: سی تی اسکن<br/>06: ام آر آی<br/>07: پزشکی هسته ای<br/>08: رادیوتراپی<br/>09: ادیومتری<br/>10: گاتار درمانی<br/>11: آنژیوگرافی<br/>12: خدمات مکمل بر اقدامات تشخیصی<br/>13: فیزیوتراپی<br/>14: سنجش تراکم استخوان<br/>15: دیالیز<br/>16: ویزیت<br/>17: خدمت پزشکی و خدمت دندانپزشکی<br/>21: پرستاری |

##### ساختار پارامتر TbDrugAmount

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| drugAmntId | Integer | مقادیر قابل دسترس این فیلد در قسمت سرویس های اطلاعات پایه آمده است. |

##### ساختار پارامتر DrugInstruction

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| drugInstId | Integer | مقادیر قابل دسترس این فیلد در قسمت سرویس های اطلاعات پایه آمده است. |

##### ساختار پارامتر ParTarefGroup

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| parGrpCode | String | 001: آسیب شناسی<br/>002: ژنتیک<br/>003: گلوبال ژنتیک<br/>000: نامشخص<br/>004: تشخیص طبی |

##### ساختار پارامتر NoteDetailsReferral

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| id | Long | |
| referralHijriDate | String | تاریخ ارجاع نسخه (مانند 14040510) |
| patientNationalCode | String | کدملی بیمار |
| docSpecReferred | Special | تخصص ارجاع گیرنده (نحوه ارسال داده در مثال های زیر آمده است). |
| message | String | متن نسخه ارجاع |
| icd10s | List<Icd10> | تشخیص اولیه (مقادیر آن از طریق سرویس که در بخش سرویس های اطلاعات پایه آمده، قابل دسترس است) |
| complaints | List<Complaint> | شکایات (مقادیر آن از طریق سرویس که در بخش سرویس های اطلاعات پایه آمده، قابل دسترس است) |
| quantity | int | تعداد ارجاع |
| siamId | String | شناسه سیام درمانگاه یا بیمارستانی که پزشک جهت بستری نسخه دستور بستری را ثبت می کند. (باید فقط برای نسخ دستور بستری ارسال گردد.) |

##### ساختار پارامتر Complaint

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| complaintIDs | String | شناسه فیلدهای شکایت که مقادیر آن از طریق سرویس مرتبط در بخش سرویسهای اطلاعات پایه در دسترس می باشد. امکان ثبت چند شکایت وجود دارد که باید با "،" از هم جدا گردد. |

##### ساختار پارامتر DiagnosisID

| نام | نوع | توضیحات |
| :--- | :--- | :--- |
| icdId | Integer | شناسه فیلد تشخیص اولیه که مقادیر آن از طریق سرویس مرتبط در بخش سرویسهای اطلاعات پایه در دسترس می باشد. |

در فراخوانی سرویس ثبت نسخ الکترونیکی، به موارد زیر توجه داشته باشید:

1.  سه قلم اطلاعاتی `docMobileNo` و `docNationalCode` و `docId` صرفا جهت کنترل های مرتبط با احراز هویت پزشک ثبت نام شده می باشد و لذا نیاز است حتما با اطلاعات وارد شده هنگام ثبت نام پزشک مطابقت داشته باشد.
2.  جهت ثبت نسخه دارویی با دوره تکرار، علاوه بر ارسال دوره تکرار (repeat)، باید تاریخ انجام (dateDo) نیز ارسال شود. تکرار نسخه به این صورت خواهد بود که نسخه به تعداد دوره تکرار و به فاصله زمانی تاریخ امروز و تاریخ انجام (ارسال شده از طریق سرویس) تکرار خواهد شد. توجه داشته باشید فرمت ارسالی تاریخ انجام، باید به صورت رشته با طول هشت کاراکتر (همانند تاریخ نسخه، مانند 14040610) باشد و امکان تکرار بیشتر از 3 دوره وجود ندارد. همچنین جهت ثبت نسخه پاراکلینیک در صورتی که تاریخ موثر برای خدمتی وارد شود (همان فیلد dateDo)، پس از آن تاریخ امکان اقدام بر روی آن خدمت در مراکز پاراکلینیک وجود دارد.
3.  در نسخ فیزیوتراپی امکان مشخص کردن نوع بیماری و طرح درمان نیز وجود دارد، که باید شناسه مربوط به هر کدام ارسال شود. امکان دریافت شناسه هر کدام از طریق سرویس مربوطه که در لیست سرویس های پایه قرار دارد میسر است.
4.  در نسخ دندانپزشکی از فیلد `isDentalService` برای دریافت "نوع خدمت دندانپزشکی" بیمار استفاده شده است که به صورت رشته (string) با محدوده مقادیر 1 و یا 0 ارسال میگردد. مقدار "1" مربوط به خدمتهایی است که مشخصا برای یک دندان انجام شده است ولی مقدار "0" مربوط به خدمتهایی است که به شماره دندان بستگی ندارند و به کلیه ی دندانها می توانند تعلق گیرند. توجه بفرمایید پارامتر `isDentalService` اجباری بوده و در صورت عدم وجود آن در نسخه دندانپزشکی، ثبت نسخه امکان پذیر نخواهد بود. در صورتی که سرویس وابسته به دندان خاصی باشد بایستی کد `FDI` آن دندان در قالب فیلد `toothId` ارسال گردد. نمونه داده زیر در خصوص پارامترهای سرویس ثبت نسخ دندانپزشکی می باشد.

    ```json
    {
      "patient": "*********",
      "mobile": "*********",
      "presctype": { "presctypeId": 5 },
      "prescDate": "14010818",
      "docId": "000000000",
      "docMobileNo": "09120000000",
      "docNationalCode": "00000000000",
      "comments": "test",
      "expireDate": "14011018",
      "creatorType": "4",
      "noteDetailEprscs": [
        {
          "srvId": { "srvType": { "srvType": "17" }, "srvCode": "046386-901" },
          "srvQty": 1,
          "isDentalService": 1,
          "toothId": "11"
        }
      ]
    }
    ```

5.  فیلدهای مربوط به شکایت و تشخیص اولیه باید به صورت رشته و کد هر کدام در سرویس ارسال گردد. (کدها از طریق سرویس اطلاعات پایه شکایت و تشخیص اولیه در دسترس می باشد.) توجه داشته باشید در صورت ارسال شناسه هر کدام از فیلدهای ذکر شده در ثبت نسخه با خطا مواجهه خواهید شد و باید همانند مثال بالا ارسال شوند.
6.  در ثبت نسخه بازخورد ارجاع (prescType=8) ثبت توضیحات ارجاع در قلم اطلاعاتی comments الزامی می باشد.
7.  در صورت ثبت بازخورد ارجاع، می بایست شناسه نسخه ای اولیه (نسخه ای می باشد که پزشک دوم به عنوان گیرنده ارجاع می خواهد بر روی آن بازخورد ثبت نماید) که خواهان ثبت بازخورد می باشیم بر روی قلم `referralFeedbackId` ارسال گردد. در زیر یک نمونه از داده ی ارسالی سرویس جهت ثبت نسخه بازخورد ارجاع پزشک را مشاهده می کنید:

    ```json
    {
      "patient": "**********",
      "prescType": { "prescTypeId": 8 },
      "prescDate": "14021011",
      "docId": "**********",
      "docMobileNo": "**********",
      "docNationalCode": "**********",
      "expireDate": "14021013",
      "noteDetailEprscs": [],
      "referralFeedbackId": 1028,
      "comments": "test"
    }
    ```

8.  برای ثبت نسخه پرستاری حداقل باید یک نسخه خدمات پزشکی، همراه با آن خدمت پرستاری ارسال شود و برای ثبت آن حتما نیاز است نسخه از درمانگاه ثبت شده باشد، پس نیاز است siamId ارسال شود. در زیر یک نمونه از داده ی ارسالی سرویس جهت ثبت نسخه خدمات پرستاری را مشاهده می کنید:

    ```json
    {
      "patient": "**********",
      "mobile": "**********",
      "prescType": { "prescTypeId": 5 },
      "prescDate": "14030106",
      "docId": "**********",
      "docMobileNo": "09110000000",
      "docNationalCode": "**********",
      "creatorType": "7",
      "siamId": "614E4BCC-8AEE-40A5-B80F-**********",
      "comments": "",
      "expireDate": "14021130",
      "clientId": "sdsdsd",
      "noteDetailEprscs": [
        { "srvId": { "srvType": { "srvType": "21" }, "srvCode": "0000903575" }, "srvQty": 1 },
        { "srvId": { "srvType": { "srvType": "21" }, "srvCode": "0000903570" }, "srvQty": 1 },
        { "srvId": { "srvType": { "srvType": "17" }, "srvCode": "0000602595" }, "srvQty": 1 }
      ]
    }
    ```

9.  برای ثبت نسخه به صورت ناشناس، فیلد patient را با مقدار `UNKNOWN` ارسال نمایید.
10. از فیلد `referenceStatus` در نسخه های ویزیت و خدمات پزشکی استفاده می شود و وضعیت ارجاع را مشخص می کند. (در صورت ارسال `referenceStatus=1` به معنی قابلیت ارجاع دارد.) در زیر یک نمونه برای این حالت ارسال شده است:

    ```json
    {
      "patient": "*********",
      "prescType": { "prescTypeId": 5 },
      "prescDate": "14010225",
      "docId": "*****",
      "docMobileNo": "**********",
      "docNationalCode": "*********",
      "comments": "",
      "noteDetailEprscs": [
        {
          "noteDetailsEprscId": 1139861,
          "srvId": { "srvType": { "srvType": "17" }, "srvCode": "0000900770" },
          "srvQty": 1,
          "referenceStatus": 1
        }
      ]
    }
    ```

11. جهت ثبت نسخه الکترونیک توسط ماما با توجه به اینکه شماره نظام پزشکی آنها دارای حرف "م" می باشد، این حرف باید به صورت "*" در آخر شماره نظامشان ارسال گردد. همچنین جهت نسخه الکترونیک توسط پزشکان اتباع باید به جای کد ملی، کد فیدا ارسال گردد و حتما قبل از کد فیدا عبارت "FDA" نیز ارسال گردد و برای فیلد شماره نظام پزشکی نیز باید حروف "ات" در آخر شماره نظامشان قرار داشته باشد.
12. جهت ثبت نسخه دستور بستری، پزشک امکان ثبت نسخه بستری برای مراکزی که پزشک در آن ها فعالیت می کند وجود دارد و باید شناسه سیام آن مرکز ارسال گردد. ضمنا همانند نسخ ارجاع باید تاریخ بستری نیز در فیلد مشخص شده ی `referralHijriDate` به صورت رشته 8 رقمی (همانند تاریخ ثبت نسخه) ارسال گردد. علاوه بر این باید تشخیص اولیه پزشک نیز همانند نمونه ارسال گردد. در زیر یک نمونه از داده ارسالی جهت ثبت نسخه دستور بستری آمده است:

    ```json
    {
      "patient": "0000000000",
      "prescType": { "prescTypeId": 9 },
      "prescDate": "14050115",
      "docId": "00000123456",
      "docMobileNo": "09121111111",
      "docNationalCode": "1234567891",
      "comments": "",
      "expireDate": "14050330",
      "noteDetailEprscs": [],
      "noteDetailsReferralList": [
        {
          "icd10s": [{ "icdId": "X46.49" }, { "icdId": "X46.51" }],
          "message": "توضیحات پزشک",
          "referralHijriDate": "14050220",
          "siamId": "A0EER--_SB3FBG"
        }
      ]
    }
    ```

## 4- سرویس استحقاق سنجی بیمار (استعلام وضعیت استحقاق)

با استفاده از این سرویس امکان استعلام وضعیت استحقاق بیمار میسر می‌باشد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/patient/v2/deserve-info/{siam-id}/doc-id}/{patient-id}`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/patients/deserve-info/{requestBy}/{siam-id}/doc-id}/{patient-id}`

سرویس با متد GET باید فراخوانی گردد. ورودی های سرویس پارامترهای زیر خواهد بود:

- **siamId**: شناسه سیام درمانگاه
- **doc-id**: کد نظام پزشک
- **patient-id**: کد ملی بیمار
- **requestBy**: کد ملی پزشک (فقط در محیط تست ارسال گردد)

**توجه داشته باشید** در صورتی که پزشک استحقاق سنجی را در مطب خود انجام می دهد به جای شناسه سیام درمانگاه، کد نظام پزشکی ارسال گردد.

**نکته مهم:** این سرویس علاوه بر نقش پزشک، برای منشی مطب و درمانگاه نیز قابل استفاده می باشد و امکان فراخوانی آن وجود دارد.

## 5- سرویس واکنش نسخه

در خروجی این سرویس جزئیات نسخه که شامل اقلام (ریز نسخه) می باشد، نمایش داده خواهد شد. این سرویس با متد GET باید فراخوانی گردد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/{headerID}/{docId}`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/ep/{headerID}/{docNationalCode}/{docId}/detail`

**ورودی های سرویس:** headerID، docId، docNationalCode

- توجه داشته باشید هر پزشک می تواند با clientId که نسخه را ثبت کرده است، واکنشی نماید.

## 6- سرویس حذف نسخه

جهت حذف نسخه الکترونیک از این سرویس استفاده میگردد که باید با متد POST فراخوانی گردد. توجه داشته باشید امکان حذف نسخه تا پایان روزی که نسخه ثبت گردیده، وجود دارد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/remove/{headerID}/{docId}`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/ep/{headerID}/{docNationalCode}/{docId}`

ورودی‌های سرویس همانند سرویس واکنشی می باشد.

## 7- سرویس ویرایش نسخه

از این سرویس جهت ویرایش نسخه تا قبل از اینکه اقدامی بر روی نسخه (اقدام از سمت داروخانه یا مراکز پاراکلینیکی) استفاده می شود که باید با متد POST فراخوانی گردد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/edit/{headerID}/{docId}`  
**آدرس محیط تست:** `https://ep-test.tamin.ir/api/v2/ep/update/{headerID}/{docNationalCode}/{docId}`

ورودی این سرویس لیست اقلام نسخه ثبت شده می باشد. اقلام نسخه در بخش ساختار پارامتر noteDetailEprscs توضیح داده شد. نحوه عملکرد سرویس به این گونه است که در این لیست اگر قلم نسخه ای که در نسخه اصلی ثبت شده بود وجود نداشته باشد، آن قلم از نسخه حذف خواهد گردید و اگر قلم جدیدی به این لیست اضافه گردد، به نسخه اضافه خواهد شد و اگر پارامتر دیگری در قلم موجود در نسخه مانند تعداد، نحوه مصرف یا ... تغییر کند، در نسخه اصلی بروزرسانی خواهد شد.

همچنین توجه داشته باشید هر نسخه توسط همان clientId که نسخه ثبت شده قابل ویرایش می باشد. هر نسخه صرفا یکبار قابل ویرایش است و نسخ از نوع ویزیت و خدمات پزشکی قابل ویرایش نیستند.

## 8- سرویس های اطلاعات پایه

تمام داده هایی که در سایر سرویس ها مطرح شده و اطلاعات پایه ای جهت ثبت نسخ می باشد در این بخش توضیح داده شده است. (آدرس اول برای هر سرویس آدرس محیط عملیاتی و بعدی آدرس محیط تست می باشد.)

- **لیست انواع نسخه الکترونیک**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/prescription-type`
    - `https://ep-test.tamin.ir/api/v2/ws-prescription-type`
- **لیست انواع ریز نسخه**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/service-type`
    - `https://ep-test.tamin.ir/api/v2/ws-service-type`
- **کدینگ دارو، پاراکلینیک و خدمات**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/services?service-type=[SERVICE_TYPE]`
    - `https://ep-test.tamin.ir/api/v2/ws-services?serviceType=[SERVICE_TYPE]`
- **لیست زیر گروه نسخ آزمایش**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/par-taref`
    - `https://ep-test.tamin.ir/api/v2/ws-par-taref`
- **لیست مقادیر مصرف دارو**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/drug-amount`
    - `https://ep-test.tamin.ir/api/v2/ws-drug-amount`
- **لیست طریقه مصرف دارو**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/drug-usage`
    - `https://ep-test.tamin.ir/api/v2/ws-drug-usage`
- **لیست زمان مصرف دارو**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/drug-instruction`
    - `https://ep-test.tamin.ir/api/v2/ws-drug-instruction`
- **لیست طرح درمان**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/ph-plan`
    - `https://ep-test.tamin.ir/api/v2/ws-ph-plan`
- **لیست انواع بیماری**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/ph-illness`
    - `https://ep-test.tamin.ir/api/v2/ws-ph-illness`
- **لیست شکایت (جهت استفاده در سرویس های ارجاع)**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/complaint`
    - `https://ep-test.tamin.ir/api/complaint`
- **لیست تشخیص اولیه**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/icd10/getAll`
    - `https://ep-test.tamin.ir/api/icd10/getAll`
- **لیست انواع تخصص پزشک**
    - `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/special/getAll`
    - `https://ep-test.tamin.ir/api/specials/getAll`

## 9 - سرویس های مرتبط با دندانپزشکی

در این بخش سرویس های مرتبط با قواعد بیزینسی دندانپزشکی توضیح داده شده است.

### 1-9 سرویس بررسی قواعد دندانپزشکی

این سرویس باید با متد POST فراخوانی گردد. هدف از این سرویس بررسی قوانین و تعرفه های دندانپزشکی جهت تعیین امکان یا عدم امکان پرداخت به پزشک می باشد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/check-rules-in-detail`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/check-rules-in-detail`

جهت فراخوانی این سرویس بایستی کد خدمت، به همراه نوع آن مطابق آنچه در ثبت نسخه انجام می گردد، به عنوان پارامترهای اصلی ارسال شوند و برخی پارامترهای دیگر که در ادامه توضیح داده خواهند شد جهت تکمیل داده های ورودی سرویس اضافه گردند. ورودی یک object از کلاس DentistRule می باشد.

در صورتی که سرویس وابسته به دندان خاصی باشد بایستی کد FDI آن دندان در قالب فیلد toothId ارسال گردد. در صورتی که خدمت انتخاب شده مربوط به رادیوگرافی باشد، آن تعداد از خدمتهایی که تا قبل از آن به نسخه جاری اضافه شده اند جهت بررسی کلیشه مشترک با خدمت جاری بایستی به ورودی سرویس ارسال گردند. این مجموعه خدمتها در قالب لیستی از کلاس GridData ارسال می شوند. برای سایر خدمتها پر بودن یا خالی بودن GridData در ورودی اهمیتی ندارد.

#### ساختار کلاس DentistRule

| نام | نوع | توضیحات | وضعیت |
| :--- | :--- | :--- | :--- |
| patientId | String | کد ملی بیمار | ★ |
| srvId | Service | کد خدمت | ★ |
| docId | String | شماره نظام پزشکی (بدون علامت) | ★ |
| toothId | String | شناسه FDI | در صورت وابستگی خدمت به دندان |
| allGridData | GridData | محتوای جدول قبل از خدمت جاری | در صورت انتخاب خدمت رادیوگرافی |

#### ساختار کلاس GridData

| نام | نوع | توضیحات | وضعیت |
| :--- | :--- | :--- | :--- |
| srvId | Service | کد خدمت | ★ |
| toothId | String | شناسه FDI | در صورت وابستگی خدمت به دندان |

شرح ورودی های این سرویس عبارتند از:

- `srvType`: نوع خدمت که برای خدمتهای دندانپزشکی بایستی 17 باشد.
- `srvCode`: کد معتبر مربوط به خدمت انتخاب شده.
- `patientId`: کد ملی بیمار
- `docId`: شماره نظام پزشکی مربوط به دندانپزشک
- `toothId`: کد FDI مربوط به دندان انتخاب شده.
- `allGridData`: محتوای گرید درج شده در نسخه مربوط به جزییات نسخه

در زیر یک نمونه از ورودی این سرویس را مشاهده می کنید:

```json
{
  "srvId": { "srvType": { "srvType": "17" }, "srvCode": "0000840462" },
  "patientId": "***********",
  "docId": "***********",
  "toothId": "12",
  "allGridData": [
    { "srvId": { "srvType": { "srvType": "17" }, "srvCode": "0000001352" }, "toothId": "12" }
  ]
}
```

### 2-9 سرویس های پایه مربوط به دندانپزشکی

در ثبت نسخه های دندانپزشکی، بر اساس نوع خدمت دندانپزشکی یکی از دو سرویس زیر جهت نمایش خدمت های قابل ارائه، فراخوانی می شود:

نوع خدمت دندانپزشکی با این دامنه در سیستم تعریف شده است:
1. خدمات و مراقبت های سلامت با کد 1
2. خدمات دندانپزشکی با کد 2 (خدمات رادیوگرافی از جمله بایت وینگ و پری اپیکال در این دسته خدمت قرار می گیرند.)

#### 1-2-1 سرویس اول

در صورتی که نوع خدمت با کد 1 انتخاب شود، لیست خدمات قابل ارائه از طریق وب سرویس زیر فراخوانی می شود. با فراخوانی این سرویس دندانپزشک می تواند لیست خدمات مرتبط با مراقبت های سلامت دندان و رادیوگرافی را دریافت کند. این سرویس با متد GET باید فراخوانی گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/find-dentist-service-name-without-tooth/{dentistType}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/ws-dentist-base/find-dentist-service-name-without-tooth/{dentistType}`

**ورودی های این سرویس:** `dentistType`: نوع خدمت دندانپزشکی (که برای خدمات و مراقبت های سلامت کد 1 می باشد.)

#### 2-2-1 سرویس دوم

در صورتی که نوع خدمت با کد 2 انتخاب شود، لیست خدمات قابل ارائه از طریق وب سرویس زیر فراخوانی می شود. با فراخوانی این سرویس دندانپزشک می تواند لیست خدمات مرتبط با یک دندان خاص را دریافت کند. (که شامل خدمتهای رادیوگرافی نیز می باشد.) این سرویس با متد GET باید فراخوانی گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/find-dentist-service-name-by-tooth/{selectedTooth}/{patientId}/{toothType}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/ws-dentist-base/find-dentist-service-name-by-tooth/{selectedTooth}/{patientId}/{toothType}`

**پارامترهای ورودی:**
- `selectedTooth`: کد FDI دندان انتخاب شده جهت ارائه خدمت
- `patientId`: کد ملی بیمار
- `toothType`: نوع دندان شامل شیری (با کد 1) یا دائمی (با کد 2)

## 10 - سرویس های مرتبط با ارجاع

با توجه به توضیحات ارائه شده درباره نسخه ارجاع و نحوه ثبت آن در این بخش سرویس های مرتبط با نظام ارجاع شرح داده شده است.

### 1-10 سرویس واکشی تعداد نسخه باز

هدف از این سرویس بررسی وجود یا عدم وجود نسخه ارجاعی باز با تخصص پزشک می باشد. این سرویس باید با متد GET فراخوانی گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/noteDetailsReferral/count/{nationalCode}/{docId}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/referral/count/{nationalCode}/{docId}`

**نکته مهم:** این سرویس علاوه بر نقش پزشک، برای منشی مطب و درمانگاه نیز قابل استفاده می باشد و امکان فراخوانی آن وجود دارد.

### 2-10 سرویس واکشی نسخه ارجاع پزشک

با استفاده از این سرویس امکان واکشی و مشاهده جزئیات نسخه ارجاع وجود دارد. این سرویس باید با متد GET فراخوانی گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/findNoteReferral/{nationalCode}/{docId}/{trackingCode}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/ep/findNoteReferral/{nationalCode}/{docId}/{trackingCode}`

**پارامترهای ورودی:**
- `nationalCode`: کد ملی بیمار
- `docId`: شماره نظام پزشکی
- `trackingCode`: کد رهگیری نسخه

### 3-10 سرویس واکنش نسخ دارای بازخورد ارجاع

با استفاده از این سرویس امکان دریافت لیست نسخ دارای بازخورد ارجاع وجود دارد. این سرویس باید با متد GET فراخوانی گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/findNoteReferral/{masterId}/{nationalCode}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/ep/findNoteReferral/{masterId}/{nationalCode}`

**پارامترهای ورودی:**
- `nationalCode`: کد ملی بیمار
- `masterId`: شناسه نسخه ارجاع داده شده

- مقدار فیلد masterId از طریق سرویس واکنشی نسخه ارجاع پزشک قابل دسترس می باشد. در خروجی سرویس ذکر شده فیلد masterParent قرار دارد که باید به عنوان ورودی این سرویس ارسال شد.

### 4-10 سرویس دریافت لیست نسخ ارجاعی پزشک در 3 روز اخیر

با استفاده از این سرویس امکان دریافت لیست نسخ ارجاعی پزشک قابل دریافت می باشد. (در 3 روز اخیر)

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/noteDetailsReferral/referredList/{nationalCode}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/referral/referredList/{nationalCode}`

**پارامترهای ورودی:**
- `nationalCode`: کد ملی پزشک

### 5-10 سرویس های نمایش بازخورد ارجاع

در این قسمت سرویس های واکنشی نسخ بازخورد ارجاع ثبت شده برای بیماران شرح داده شده است.

#### 1-5-10 سرویس واکنشی نسخه ارجاع ثبت شده بیمار

این سرویس با دریافت شماره ملی پزشک، شماره ملی بیمار و کدپیگیری نسخه ثبت شده برای بیمار، نسخ ارجاع ثبت شده برای بیمار را واکنشی کرده و امکان مشاهده جزئیات آن نسخه را فراهم می کند. این سرویس با متد GET فراخوانی گردد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/fetchReferralCartable/{docNationalCode}/{patientNationalCode}/{trackingCode}`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/cartablenoteDetailsReferral/{docNationalCode}/{patientNationalCode}/{trackingCode}`

نمونه زیر، یک نمونه موفق خروجی این سرویس می باشد:

```json
{
  "status": 200,
  "family": "SUCCESSFUL",
  "reason": "OK",
  "data": {
    "total": 1,
    "list": [
      {
        "noteHeadId": "شناسه ی یکتا نسخه",
        "referralId": "شناسه ارجاع ثبت شده",
        "patientID": "کد ملی پزشک",
        "docID": "شماره نظام پزشک"
      }
    ]
  }
}
```

در خروجی های این سرویس منظور از پزشک ارجاع دهنده پزشک اول می باشد که نسخه ارجاع را ثبت کرده و بیمار را به پزشکی با تخصص دیگر ارجاع داده است. همچنین منظور از پزشک ارجاع شده، پزشکی است که بیمار به آن مراجعه نموده و در واقع در این چرخه پزشک دوم می باشد که بیمار به وی مراجعه کرده است. توجه داشته باشید از فیلد شناسه ارجاع ثبت شده در سرویس های بعدی جهت دریافت جزئیات این نسخه استفاده خواهیم کرد.

#### 2-5-10 سرویس دریافت لیست نسخ بازخورد ارجاع ثبت شده

با کمک این سرویس امکان دریافت لیست بازخوردهای ثبت شده برای نسخه ارجاع وجود دارد. پس از فراخوانی سرویس قبل و دریافت اطلاعات نسخه ارجاع، با کمک این سرویس امکان دریافت بازخوردهای ثبت شده برای ارجاع وجود دارد. این سرویس نیز به صورت GET فراخوانی میگردد و تنها ورودی سرویس شناسه ارجاع ثبت شده می باشد که در خروجی سرویس قبل آمده است.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/referralList/1/referralId`

در خروجی این سرویس یک لیست برگردانده می شود که شامل نسخی است که پزشک در بازخورد ارجاع ثبت شده ثبت کرده است. در زیر یک نمونه از خروجی فراخوانی موفق این سرویس آمده است. (در نمونه خروجی زیر در کنار هر فیلد از خروجی توضیحات آن فیلد شرح داده شده است.)

```json
{
  "status": 200,
  "family": "SUCCESSFUL",
  "reason": "OK",
  "data": {
    "total": 2,
    "list": [
      {
        "id": 327241062,
        "masterParent": 327240269,
        "masterChild": 327247893,
        "referralDate": 1750517662000,
        "docId": "2000200090",
        "quantity": 1,
        "specCodeReferral": "00100",
        "specDescReferral": "پزشک عمومی",
        "specCodeReferred": "00138",
        "specDescReferred": "متخصص قلب و عروق",
        "icd10Referrals": null,
        "complaints": null,
        "message": "بیماری تشخیص داده شده و توضیحات",
        "diagnosisIDs": "W27.19"
      },
      {
        "id": 0,
        "masterParent": 327247893,
        "masterChild": null,
        "referralDate": 1747839599371,
        "docId": null,
        "quantity": 0,
        "specCodeReferral": "00138",
        "specDescReferral": "متخصص قلب و عروق",
        "specCodeReferred": null,
        "specDescReferred": null,
        "icd10Referrals": null,
        "complaints": null,
        "message": null,
        "diagnosisIDs": null
      }
    ]
  }
}
```

توجه داشته باشید پزشک دوم یا پزشکی که بازخورد را ثبت می کند، علاوه بر ثبت بازخورد ارجاع امکان ثبت سایر نوع نسخ (دارو، پاراکلینیک، تصویربرداری، خدمات پزشکی و ارجاع) را نیز دارد و در خروجی سرویس مواردی که id آن مقدار غیر صفر داشته باشد، یکی از نوع نسخ ذکر شده است که پزشک ثبت کرده، اما مواردی که مقدار این فیلد صفر باشد، بازخورد پزشک دوم در ارجاع می باشد.

#### 3-5-10 سرویس دریافت جزئیات بازخورد ارجاع ثبت شده

سرویس قبل، امکان دریافت لیست نسخی که پزشک در بازخورد یک ارجاع ثبت کرده را فراهم می نماید. در این سرویس نیز امکان دریافت جزییات هر کدام از نسخ ثبت شده برای بازخورد ارجاع وجود دارد. نحوه فراخوانی این سرویس نیز باید با متد GET باشد و به این گونه است که باید فیلدهای id, masterParent که در خروجی سرویس قبل وجود دارد به عنوان ورودی ارسال گردد تا جزییات هرکدام از نسخه مشاهده گردد.

**آدرس وب سرویس عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/referral/noteDetail/{id}/{masterParent}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/noteDetailsReferral/noteheads/{id}/{masterParent}`

**نمونه فراخوانی:**
- `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/referral/noteDetail/327241062/327240269`
- `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/referral/noteDetail/0/327247893`

نمونه خروجی سرویس در حالت اول (ترکیبی از دارو، خدمات و بازخورد ارجاع):

```json
{
  "status": 200,
  "family": "SUCCESSFUL",
  "reason": "OK",
  "data": {
    "total": 2,
    "list": [
      {
        "noteHeadEprscId": 9149887014,
        "patient": "00000000000",
        "prescType": { "prescTypeId": 1, "prescTypeCode": "01", "prescTypeDesc": "دارو" },
        "prescDate": null,
        "comments": null,
        "noteDetailEprscs": [
          {
            "noteDetailsEprscId": 9149887015,
            "srvId": {
              "srvId": 63970,
              "srvType": { "srvType": "01", "srvTypeDes": "داروی", "status": "1", "statusstDate": "13940101", "custType": "3", "prescTypeId": 1, "headExpireDate": 62 },
              "srvCode": "50610",
              "srvName": "DOXORUBICIN HYDROCHLORIDE (PEGYLATED) 2 mg/1mL 10",
              "srvName2": null,
              "srvBimSw": "1",
              "srvSex": null,
              "srvPrice": 8000000,
              "srvPriceDate": "13990723",
              "doseCode": null,
              "formCode": { "formCode": "17", "formDes": "INJECTION", "formGrp": null, "status": "1", "statusstDate": "20020101" },
              "parTarefGrp": null,
              "status": "1",
              "statusstDate": "13961027",
              "bGType": "1",
              "gSrvCode": null,
              "agreementFlag": null,
              "isDeleted": "0",
              "visible": "1",
              "dentalServiceType": null,
              "wsSrvCode": "50610",
              "hosprescType": "0",
              "srvRule": null,
              "countIsRestricted": null,
              "drugWarning": "0",
              "terminology": null,
              "srvCodeComplete": "0000050610"
            },
            "srvQty": 6,
            "dateDo": null
          }
        ],
        "noteDetailsReferralList": null
      },
      {
        "noteHeadEprscId": 9149898411,
        "patient": "1531257747",
        "prescType": { "prescTypeId": 7, "prescTypeCode": "07", "prescTypeDesc": "ارجاع" },
        "prescDate": null,
        "comments": null,
        "noteDetailEprscs": [],
        "noteDetailsReferralList": [
          {
            "id": 327241062,
            "masterParent": 327240269,
            "masterChild": 327247893,
            "referralDate": 1750517662000,
            "docId": "2000200090",
            "quantity": 1,
            "specCodeReferral": "00100",
            "specDescReferral": "پزشک عمومی",
            "specCodeReferred": "00138",
            "specDescReferred": "متخصص قلب و عروق",
            "icd10s": [
              {
                "icdId": "W27.19",
                "srvId": {
                  "srvType": { "srvType": "02" },
                  "srvCode": "030246-300",
                  "srvName": "30246-3 - CMV - /PCR ???? ???? CMV /???",
                  "srvName2": "30246-3 - CMV - Cytomegalovirus DNA [Presence] in ",
                  "srvBimSw": "1",
                  "srvPrice": 564650,
                  "srvPriceDate": "13990201",
                  "parTarefGrp": { "parGrpCode": "002", "parGrpDesc": "", "parGrpRem": "1", "status": "1", "statusStDate": "13830101" },
                  "srvCodeComplete": "030246-300"
                }
              }
            ],
            "complaints": [],
            "message": null,
            "diagnosisIDs": null
          }
        ]
      },
      {
        "noteHeadEprscId": 9149982811,
        "patient": "1531257747",
        "prescType": { "prescTypeId": 8, "prescTypeCode": "08", "prescTypeDesc": "" },
        "prescDate": null,
        "comments": "توضیحات پزشک در باز خورد ارجاع انجام شده",
        "noteDetailEprscs": [],
        "noteDetailsReferralList": null
      }
    ]
  }
}
```

توجه داشته باشید در نظام ارجاع، امکان ثبت ارجاع مجدد توسط پزشک دوم به یک تخصص دیگر نیز وجود دارد و همانطور که در خروجی این سرویس مشاهده کردید، امکان ثبت ارجاع برای پزشکی که بیمار به آن مراجعه کرده است، نیز وجود دارد.

### 6-10 سرویس فراخوانی نسخ باز ارجاعی بیمار توسط منشی

هدف این سرویس این است که منشی با ارسال کدملی بیمار ارجاعات باز بیمار به متخصص های مختلف رو نمایش داده که با توجه به درخواست بیمار جهت یکی از تخصص ها پذیرش می گردد.

**آدرس وب سرویس:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/patientNoteDetailsReferral/count/{nationalCode}`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/referral/count/{nationalCode}`

**پارامترهای ورودی:**
- `nationalCode`: کد ملی بیمار

**نمونه خروجی سرویس:**

```json
{
  "status": 200,
  "family": "SUCCESSFUL",
  "reason": "OK",
  "data": {
    "noteDetailsReferrals": [
      {
        "specCode": "00199",
        "specDesc": "بیولوژي پزشکي",
        "specGRP": "6",
        "docComment": null,
        "status": "1",
        "statusstDate": "13900101",
        "typeSpec": null,
        "maxNormal": null,
        "maxSpecial": null,
        "lstatus": "0"
      }
    ],
    "count": 1
  }
}
```

## 11 - سرویس های مرتبط با کارتابل پرستاری

امکان ثبت خدمات پرستاری توسط پزشک در سرویس ثبت نسخه وجود دارد و خدماتی مانند مراقبت های پرستاری از این طریق ثبت می شوند که در بخش ثبت نسخه به همراه نمونه ورودی شرح داده شد. پرستار درمانگاه جهت مشاهده نسخی که دارای خدمات پرستاری می باشند و پرستار دیگری روی آن اقدام نکرده باشد، باید از سرویس واکنشی نسخ پرستاری استفاده نماید. همچنین پرستار جهت ثبت اقدامات انجام شده روی نسخه ثبت شده، از سرویس ثبت اقدام روی نسخه پرستاری که در ادامه شرح داده شده است، استفاده نماید.

### 1-11 سرویس واکنش نسخ پرستاری درمانگاه

هدف این سرویس این است که با دریافت شناسه سیام درمانگاه، نسخی که دارای خدمات پرستاری باشند و پرستار دیگری روی آن اقدامی نکرده باشد، را در خروجی برمیگرداند. این سرویس باید با متد GET فراخوانی گردد.

**آدرس وب سرویس:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/cartableNurse/getAll/{siamId}//nationalCode1`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/api/v2/cartable-nurse/get-all/{siamId}//nationalCode`

**پارامترهای ورودی:**
- `siamId`: شناسه سیام درمانگاه
- `nationalCode`: کد ملی بیمار

### 2-11 سرویس ثبت اقدام روی نسخه پرستاری

با استفاده از سرویس قبل، امکان مشاهده نسخ پرستاری وجود دارد، با استفاده از این سرویس امکان اعلام اقدام انجام شده روی ریز نسخه وجود دارد. این سرویس باید با متد POST فراخوانی گردد.

**پارامترهای ورودی:**

| نام | نوع | توضیحات | وضعیت |
| :--- | :--- | :--- | :--- |
| siamId | String | شناسه درمانگاه | ★ |
| noteDetailsEprscIds | List<Long> | لیست شناسه ریز نسخ (در خروجی سرویس قبل وجود دارد) | ★ |
| nurseNationalCode | String | کد ملی پرستار | ★ |

**آدرس وب سرویس:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/cartableNurse/saveEprsc`  
**آدرس وب سرویس تستی:** `https://ep-test.tamin.ir/ep/api/v2/cartable-nurse/save`

در زیر یک نمونه از ورودی سرویس را مشاهده می کنید:

```json
{
  "noteDetailsEprscIds": [1290361],
  "nurseNationalCode": "********",
  "siamId": "614E4BCC-8AEE-******-******"
}
```

## 12 - سرویس های مرتبط با خدمات ارجاعی

همانطور که در سرویس ثبت نسخه و نکات مربوط آن (مورد 10) درباره ثبت نسخ خدمات ارجاعی توضیح داده شد، امکان ثبت خدمت انجام شده توسط پزشک دوم نیز وجود دارد. حال در صورتی که برای بیمار نسخه خدمات پزشکی ارجاعی ثبت شده باشد امکان مشاهده آن نسخه باید برای پزشک دوم که در واقع انجام دهنده آن خدمت می باشد، باید میسر باشد که از طریق سرویس زیر این امکان فراهم شده است.

### 1-12 سرویس واگشی نسخه خدمات ارجاعی

با استفاده از این سرویس امکان واکشی نسخه خدمات ارجاعی ثبت شده برای بیمار وجود دارد. این سرویس با متد GET فراخوانی می شود.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/{nationalCode}/{docId}/{trackingCode}/referentialPrescDetail`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/ep/{nationalCode}/{docNatioanlCode}/{docId}/{trackingCode}/detail`

**پارامترهای ورودی:**
- `nationalCode`: کد ملی بیمار
- `docId`: شماره نظام پزشکی
- `trackingCode`: کد پیگیری نسخه ای که برای بیمار ثبت شده است.

## 13 - سرویس های مرتبط با بستری

### 1-13 سرویس واکنش لیست نسخ بستری در درمانگاه

با استفاده از این سرویس امکان مشاهده نسخ دستور بستری ثبت شده در یک مرکز درمانی را مشاهده کرد. امکان فراخوانی این سرویس فقط برای کاربران منشی وجود دارد. این سرویس باید با متد GET فراخوانی گردد.

**آدرس محیط عملیاتی:** `https://soa.tamin.ir/interface/epresc/SendEpresc/v2/hospitalization-order/fetch/{siam-id}/{secretaryNationalCode}`  
**آدرس محیط تستی:** `https://ep-test.tamin.ir/api/v2/noteDetailsReferral/hospitalization-order/{siam-id}/{secretaryNationalCode}`

**پارامترهای ورودی:**
- `siam-id`: شناسه سیام مرکز
- `secretaryNationalCode`: کد ملی منشی مرکز

## 14 - نکات مهم

- برای ارسال نسخ پزشکان، هر پزشک میبایست حتما پس از ارسال نسخ آن در سامانه نسخه الکترونیک EP.TAMIN.IR در قسمت ثبت نام پزشکان، ثبت نام و تایید شده باشند.
- برای انجام عملیات پیاده سازی و تست نیاز است `client_id` به مسئول فنی شرکت منتخب تحویل گردد. همچنین شرکت باید آدرس بازگشتی (redirect url) خود را اعلام نمایند.
- تاریخ نسخه نمی تواند از تاریخ روز بزرگتر باشد.
- اگر نوع نسخه ویزیت باشد (3)، noteDetailEprsc (اقلام نسخه) نباید دارای مقدار باشد، در غیر این صورت پیغام خطای "امکان انتخاب خدمات با توجه به نوع نسخه ی انتخابی وجود ندارد" نمایش داده می شود.
- اگر نوع نسخه دارو باشد (1)، فیلد srType باید برابر با "01" باشد در غیر این صورت پیغام خطای "با توجه به نوع نسخه ی انتخابی تنها امکان انتخاب دارو وجود دارد" نمایش داده می شود.
- اگر نوع نسخه پاراکلینیک باشد (2)، فیلد srType باید برابر با یکی از مقادیر 02, 03, 04, 05, 06, 07 باشد در غیر این صورت پیغام خطای "با توجه به نوع نسخه ی انتخابی تنها امکان انتخاب خدمات پاراکلینیک وجود دارد" نمایش داده می شود.
- اگر نوع خدمات آزمایشگاه باشد (srType برابر با 02)، در این صورت فیلد parGrpCode (گروه آزمایش) باید دارای مقدار باشد در غیر این صورت پیغام خطای "در صورت انتخاب آزمایشگاه، زیر گروه مربوطه باید انتخاب گردد" نمایش داده خواهد شد.
- اگر نوع نسخه خدمات باشد (4)، در این صورت نوع خدمات باید 17 (خدمات جنبی) باشد در غیر این صورت پیغام خطای "با توجه به نوع نسخه ی انتخابی تنها امکان انتخاب خدمات وجود دارد" نمایش داده خواهد شد.
- در صورت وجود اقلام نسخه، فیلد تعداد خدمت (srvQty) باید بزرگتر از صفر باشد، در غیر این صورت خطای "تعداد خدمت باید بزرگتر از صفر باشد" نمایش داده خواهد شد.
- در صورتی که نوع نسخه دارو (1) باشد، فیلدهای drugInstruction و timesAday مطابق با سرویس های مربوطه مقدار دهی می شوند.
- ثبت نام و فعال بودن پزشک در سامانه الزامی می باشد. در غیر این صورت پیغام خطای "پزشک با شماره نظام پزشکی مورد نظر یافت نشد" یا "پزشک فعال نشده است" نمایش داده می شود.
- کدملی و شماره تلفن همراه پزشک باید با اطلاعاتی که زمان ثبت نام در سامانه پزشکان ثبت شده است مطابقت داشته باشد در غیر این صورت پیغام خطای "کدملی/ تلفن همراه پزشک معتبر نمی باشد" نمایش داده می شود.
- زمانی که اطلاعات دریافتی خالی باشد پیام خطای "خطا در اطلاعات ورودی data is null" نمایش داده می شود.
- در صورتی که فیلد موبایل (mobile) خالی باشد، پیام خطای "شماره تلفن همراه وارد نشده است" نمایش داده می شود.
- در صورتی که فیلد موبایل (mobile) با الگوی درست وارد نشده باشد، پیام خطای "شماره تلفن همراه به درستی وارد نشده است" نمایش داده می شود.
- در صورتی که فیلد کد ملی بیمار (patient) معتبر نباشد، پیام خطای "کد ملی بیمار معتبر نمی باشد" نمایش داده می شود.
- در صورتی که فیلد svrCode معتبر نباشد، پیام خطای "کد خدمت + svrCode + تعریف نشده است" نمایش داده می شود.
- در صورتی که فیلد prsType خالی باشد، پیام خطای "نوع نسخه انتخاب نشده است" نمایش داده می شود.
- در صورتی که فیلد prsType یکی از موارد ذکر شده در سند نباشد پیام خطای "نوع نسخه بدرستی وارد نشده است" نمایش داده می شود.
- در صورتی که اطلاعات نسخه شامل کد ملی بیمار، شماره نظام پزشکی پزشک، تاریخ نسخه، نوع نسخه و اقلام نسخه تکراری باشد پیام خطای "نوع نسخه تکراری است" نمایش داده می شود.
- در صورتی که فرمت تاریخ نسخه درست نباشد پیام خطای "تاریخ به درستی وارد نشده است" نمایش داده می شود.
- در صورتی که تاریخ نسخه بزرگتر از تاریخ روز باشد پیام خطای "تاریخ نسخه نمی تواند از تاریخ روز بزرگتر باشد" نمایش داده می شود.
- در صورتی که فیلد drugAmntId به درستی وارد نشده باشد پیام "مقادیر مصرف صحیح وارد نشده است" نمایش داده می شود.
- در صورتی که فیلد drugInstId به درستی وارد نشود پیام "زمان مصرف صحیح وارد نشده است" نمایش داده می شود.
